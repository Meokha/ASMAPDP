@model IEnumerable<SIMS.Models.Course>

@{
    ViewData["Title"] = "Course List";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h1>@ViewData["Title"]</h1>
    @if (User.IsInRole("Teacher"))
    {
        <a asp-action="Create" class="btn btn-primary">Add New Course</a>
    }
</div>

@if (Model != null && Model.Any())
{
    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Course Name</th>
                <th>Description</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.Name)</td>
                    <td>@Html.DisplayFor(modelItem => item.Description)</td>
                    <td class="text-end">
                        @* Nút "Chi tiết" hiển thị cho tất cả mọi người *@
                        <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-info">View Details</a>

                        @* Các nút quản lý chỉ hiển thị cho Teacher *@
                        @if (User.IsInRole("Teacher"))
                        {
                            <text> | </text>
                            <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-warning">Edit</a>
                            <text> | </text>
                            <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger">Delete</a>
                        }
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <div class="alert alert-info">No courses available to display.</div>
}